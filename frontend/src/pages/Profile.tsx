import React from 'react';

const Profile: React.FC = () => {
    return (
        <div>
            <h1>User Profile</h1>
            {/* Add user profile details and edit functionality here */}
        </div>
    );
};

export default Profile;